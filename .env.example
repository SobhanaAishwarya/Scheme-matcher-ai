# OPTIONAL. Copy this file to ".env" and fill in ONE key to enable LLM explanations.
# Leave everything empty to run in offline "template mode" (no API key needed).
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
# Optional model overrides
# ANTHROPIC_MODEL=claude-sonnet-5
# OPENAI_MODEL=gpt-4o-mini
